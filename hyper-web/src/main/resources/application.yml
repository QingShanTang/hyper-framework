hyper:
  logging:
    home: ${user.home}/hyper/${hyper.env}/logs
server:
  port: 9999
  servlet:
    context-path: /hyper
spring:
  profiles:
    active: @env@
    include: dao,@env@-dao
minio:
  endpoint: http://localhost:9078
  accesskey: geekplus
  secretkey: geekplus
  bucketName: heihei
